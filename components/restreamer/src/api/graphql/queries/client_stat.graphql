query StatisticsQuery {
    statistics {
        publicHost
        timestamp
        inputs {
            status
            count
        }
        outputs {
            status
            count
        }
    }
}
